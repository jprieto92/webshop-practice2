Para cargar un dump:

mysql < scriptFinal.sql --user=root --password=admin

Para realizar un dump:

mysqldump -u root --password=admin tiw > scriptFinal.sql



ALTERNATIVA


Desde el workbench, el menú de "import/expor" del lateral izquierdo.